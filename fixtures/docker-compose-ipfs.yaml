version: '3.4'

services:
  ipfs0:
    container_name: ipfs0
    image: ipfs/go-ipfs:release
    ports:
      - "4001:4001" # ipfs swarm
      - "5001:5001" # ipfs api
    # - "10080:8080" # ipfs gateway
    volumes:
      - ./ipfs/ipfs0:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1

  ipfs1:
    container_name: ipfs1
    image: ipfs/go-ipfs:release
    ports:
      - "4002:4001" # ipfs swarm
      - "5002:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs1:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs2:
    container_name: ipfs2
    image: ipfs/go-ipfs:release
    ports:
      - "4003:4001" # ipfs swarm
      - "5003:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs2:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs3:
    container_name: ipfs3
    image: ipfs/go-ipfs:release
    ports:
      - "4004:4001" # ipfs swarm
      - "5004:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs3:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs4:
    container_name: ipfs4
    image: ipfs/go-ipfs:release
    ports:
      - "4005:4001" # ipfs swarm
      - "5005:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs4:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs5:
    container_name: ipfs5
    image: ipfs/go-ipfs:release
    ports:
      - "4006:4001" # ipfs swarm
      - "5006:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs5:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs6:
    container_name: ipfs6
    image: ipfs/go-ipfs:release
    ports:
      - "4007:4001" # ipfs swarm
      - "5007:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs6:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs7:
    container_name: ipfs7
    image: ipfs/go-ipfs:release
    ports:
      - "4008:4001" # ipfs swarm
      - "5008:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs7:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs8:
    container_name: ipfs8
    image: ipfs/go-ipfs:release
    ports:
      - "4009:4001" # ipfs swarm
      - "5009:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs8:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs9:
    container_name: ipfs9
    image: ipfs/go-ipfs:release
    ports:
      - "4010:4001" # ipfs swarm
      - "5010:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs9:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs10:
    container_name: ipfs10
    image: ipfs/go-ipfs:release
    ports:
      - "4011:4001" # ipfs swarm
      - "5011:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs10:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs11:
    container_name: ipfs11
    image: ipfs/go-ipfs:release
    ports:
      - "4012:4001" # ipfs swarm
      - "5012:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs11:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs12:
    container_name: ipfs12
    image: ipfs/go-ipfs:release
    ports:
      - "4013:4001" # ipfs swarm
      - "5013:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs12:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs13:
    container_name: ipfs13
    image: ipfs/go-ipfs:release
    ports:
      - "4014:4001" # ipfs swarm
      - "5014:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs13:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs14:
    container_name: ipfs14
    image: ipfs/go-ipfs:release
    ports:
      - "4015:4001" # ipfs swarm
      - "5015:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs14:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs15:
    container_name: ipfs15
    image: ipfs/go-ipfs:release
    ports:
      - "4016:4001" # ipfs swarm
      - "5016:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs15:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs16:
    container_name: ipfs16
    image: ipfs/go-ipfs:release
    ports:
      - "4017:4001" # ipfs swarm
      - "5017:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs16:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs17:
    container_name: ipfs17
    image: ipfs/go-ipfs:release
    ports:
      - "4018:4001" # ipfs swarm
      - "5018:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs17:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs18:
    container_name: ipfs18
    image: ipfs/go-ipfs:release
    ports:
      - "4019:4001" # ipfs swarm
      - "5019:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs18:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs19:
    container_name: ipfs19
    image: ipfs/go-ipfs:release
    ports:
      - "4020:4001" # ipfs swarm
      - "5020:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs19:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs20:
    container_name: ipfs20
    image: ipfs/go-ipfs:release
    ports:
      - "4021:4001" # ipfs swarm
      - "5021:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs20:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs21:
    container_name: ipfs21
    image: ipfs/go-ipfs:release
    ports:
      - "4022:4001" # ipfs swarm
      - "5022:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs21:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs22:
    container_name: ipfs22
    image: ipfs/go-ipfs:release
    ports:
      - "4023:4001" # ipfs swarm
      - "5023:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs22:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs23:
    container_name: ipfs23
    image: ipfs/go-ipfs:release
    ports:
      - "4024:4001" # ipfs swarm
      - "5024:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs23:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs24:
    container_name: ipfs24
    image: ipfs/go-ipfs:release
    ports:
      - "4025:4001" # ipfs swarm
      - "5025:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs24:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs25:
    container_name: ipfs25
    image: ipfs/go-ipfs:release
    ports:
      - "4026:4001" # ipfs swarm
      - "5026:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs25:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs26:
    container_name: ipfs26
    image: ipfs/go-ipfs:release
    ports:
      - "4027:4001" # ipfs swarm
      - "5027:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs26:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs27:
    container_name: ipfs27
    image: ipfs/go-ipfs:release
    ports:
      - "4028:4001" # ipfs swarm
      - "5028:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs27:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs28:
    container_name: ipfs28
    image: ipfs/go-ipfs:release
    ports:
      - "4029:4001" # ipfs swarm
      - "5029:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs28:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs29:
    container_name: ipfs29
    image: ipfs/go-ipfs:release
    ports:
      - "4030:4001" # ipfs swarm
      - "5030:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs29:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs30:
    container_name: ipfs30
    image: ipfs/go-ipfs:release
    ports:
      - "4031:4001" # ipfs swarm
      - "5031:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs30:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0

  ipfs31:
    container_name: ipfs31
    image: ipfs/go-ipfs:release
    ports:
      - "4032:4001" # ipfs swarm
      - "5032:5001" # ipfs api
    volumes:
      - ./ipfs/ipfs31:/data/ipfs
    environment:
      LIBP2P_FORCE_PNET: 1
    depends_on:
      - ipfs0
